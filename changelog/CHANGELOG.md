# سجل التغييرات — صِمَام (Samaam)

مشروع مقدَّم إلى **هاكاثون جاهزية الذكاء الاصطناعي ITU — المملكة العربية
السعودية**، مسار الصحة/الأشعة.

الترقيم بصيغة `MAJOR.MINOR.PATCH`. كل إصدار هنا يقابل مرحلة عمل مكتملة ومختبَرة،
لا مجرد مجموعة عشوائية من العمل. القيود الصارمة (٥ صفحات · ٧ دقائق · مستودع عام
بلا مفاتيح · بيانات محاكاة ١٠٠٪ · صفر هلوسة) لم تتغيّر منذ الإصدار الأول.

---

## v0.8.0 — سكربت الفيديو النهائي · 2026-08-30

**سكربت تصوير جديد بالإنجليزية في جذر المشروع، مبني على دليل الإخراج الرسمي
(§11) ومضغوط من ٧ دقائق إلى ٥:١٥.**

| | |
| :--- | :--- |
| **جديد** | `VIDEO-SCRIPT.md` في الجذر — ٧٣١ كلمة منطوقة، عشرة مشاهد، تعليمات شاشة لكل جملة |
| **جديد** | مشهد **نقص الأدلة** — نموذج فارغ ← `INSUFFICIENT_EVIDENCE` ← `403`. يغطّي معيار التحكيم الخامس، وكان غائباً عن السكربت السابق |
| **جديد** | مشاهد المعمارية Y.3172 وأبعاد الجاهزية ومصفوفة الثغرات — مطلوبة في دليل الإخراج ولم تكن في نسخة الأربع دقائق |
| **تغيير** | القارئ صار طبيبة، فرُفع قيد «تجنّب المصطلحات الطبية» الذي كان يحكم النسخة السابقة |
| **إصلاح** | `TECHNICAL-REPORT.md` كان يطبع `eGFR 27.7` في جدول النتائج و`19.1` في التفصيل — في الصفحة نفسها. القيمة التي يحسبها النظام فعلاً هي **19.1** |
| **إبقاء** | `submission/VIDEO-SCRIPT.md` (نسخة الأربع دقائق، بلا مصطلحات طبية) كخطة بديلة إن تعذّر التسجيل مع الطبيبة |

**تحقّق:** كل رقم في السكربت مأخوذ من خرج حيّ لا من الذاكرة — ومسار النموذج
الفارغ اختُبر عبر الـ API فعلياً (`403 · LOCKED`)، ومعه أن `SC-01` بعده يرجع
`200` رغم قفل الجهاز.

---

## v0.7.0 — الهوية البصرية · 2026-08-30

| الإصدار | الالتزام | ما تغيّر |
| :--- | :--- | :--- |
| **v0.7.2** | `7b8c5e5` | **إعادة بناء بحث قاعدة المعرفة** كصندوق مؤلِّف مع سجل في الشريط الجانبي. فُصل البحث الحيّ (تنقيط) عن السجل (التزام صريح)، فلا يمتلئ السجل بكلمات ناقصة. أُصلح عيبان قائمان: `isError` غير معالَج — الخادم يسقط والصفحة صامتة؛ ونصوص النتائج بلا `dir` فتُعرض معكوسة. **لم يُبنَ الميكروفون وزر الصوت** من اللقطة المرجعية: لا يوجد تعرّف صوتي، وزر ميت في عرض حيّ أسوأ من غيابه |
| **v0.7.1** | `3504234` | **أربع تمثيلات بصرية** — مقياس الخطر، مقارنة الجرعة، خط الضرر الزمني، مخطط الجهاز. أُضيف `readings` إلى `Check` لتصل الأرقام على السلك، وحُذف الـ regex الذي كان يستخرج `eGFR` من نصّ إنجليزي — كان على بُعد إعادة صياغة واحدة من الانكسار الصامت |
| **v0.7.0** | `abd7632` | اعتماد قالب **radix-maia** كاملاً وحذف `design/dashboard/`. ألوان الحالة مشتقّة في **OKLCH** عند إضاءة القالب نفسها. **خط واحد لكل شيء** — IBM Plex Sans Arabic بأوزانه السبعة. اكتُشف أن `--primary` في السمة الداكنة يقيس **2.03:1** كنص، فأُضيف `--primary-strong` |

---

## v0.6.0 — المراجعة الإكلينيكية · 2026-08-29

**`9cea78b` — فصل الفعل عن الحكم، بناءً على تصحيح استشارية أشعة.**

المراجِعة قالت الجملة التي غيّرت التصميم: *«أكبر شيء ممكن يرفضه الأطباء أن
النظام يأخذ القرار الطبي مكانهم.»* ولا قيمة `eGFR` تمنع الصبغة منعاً مطلقاً —
بروتوكول وزارة الصحة يجعل المرحلتين ٤ و٥ **مانعاً نسبياً**، ويمنع حرمان مريض من
استطباب مهدِّد للحياة لأسباب كلوية.

- أُضيف بُعد **الفعل** مستقلاً عن الحكم: `PROCEED` · `CONFIRM` · `AUTHORISE` ·
  `PROHIBITED`
- أُضيف بُعد **الأساس**: `STATUTORY` (مرسوم ملكي) · `NATIONAL_PROTOCOL` (وزارة
  الصحة) · `CLINICAL_GUIDANCE` — والنظام لا يخلط نظاماً بإرشاد أبداً
- أُعيد بناء السيناريو الثاني على حالتها هي: أنثى ٦٢، سرطان قولون نقيلي

---

## v0.5.0 — شاشات العرض الثلاث · 2026-08-29

| الإصدار | الالتزام | ما تغيّر |
| :--- | :--- | :--- |
| **v0.5.3** | `ea40d70` | التقرير التقني (٥ صفحات) وسكربت العرض |
| **v0.5.2** | `50425d2` | جولة الجودة — السمة الداكنة، التباين (`accent-strong` عند 5.26:1)، حلقة التركيز، الاستجابة عند 768px |
| **v0.5.1** | `9e73cb8` | شاشات قاعدة المعرفة، الثغرات، سجل التدقيق، أبعاد الجاهزية |
| **v0.5.0** | `9603963` | `PipelineRail` والكونسول وشاشة الخصوصية. **فُصل `/explain` عن القرار: من ٤٢ ثانية إلى ٢٩ مللي ثانية للرفض** — وصار هذا الفصل حجّة الفيديو الختامية بدل أن يكون عيباً |

---

## v0.4.0 — أساس الواجهة · 2026-08-29

| الإصدار | الالتزام | ما تغيّر |
| :--- | :--- | :--- |
| **v0.4.3** | `52cac5a` | الهيكل وستّ بدائل دلالية |
| **v0.4.2** | `cd176ae` | تحديد الهيكل، الصفحات الست، التدويل، السمات، وإلزام shadcn |
| **v0.4.1** | `11162a7` | خطة الواجهة وفق مهارة التصميم من Anthropic؛ **صُحِّح `DESIGN.md` و`USAGE.md` في مكانهما** (`#0C5CAB` ← `#0ea5e9`) بدل توثيق التناقض |
| **v0.4.0** | `b38b395` | اعتماد نظام التصميم؛ إضافة `POST /data/request` |

---

## v0.3.0 — الخلفية · 2026-08-29

| الإصدار | الالتزام | ما تغيّر |
| :--- | :--- | :--- |
| **v0.3.1** | `6daf5ec` | مسار Y.3172 كاملاً، بوابة الجهاز، مسار الخصوصية، مشغّل العرض |
| **v0.3.0** | `3635f1d` | **عقدة السياسات — حتمية، بلا نموذج في مسار القرار.** أربع قواعد فوق عتبات مُستشهَدة، ست أحكام، و`403` من الجهاز الوهمي |

---

## v0.2.0 — قاعدة المعرفة · 2026-08-29

| الإصدار | الالتزام | ما تغيّر |
| :--- | :--- | :--- |
| **v0.2.4** | `db30282` | تثبيت `requirements.txt` على النسخ المثبَّتة فعلاً |
| **v0.2.3** | `ac39af7` | بناء ChromaDB (٤٥ سجلاً، ٦ ثغرات، ١٣٣٨ مقطعاً). التحوّل إلى `multilingual-e5-small` بعد أن أرجع الاسترجاع العربي **لا شيء** |
| **v0.2.2** | `b69aeca` | السيناريوهات الثلاثة، وكل استشهاد مُقابَل على المتن |
| **v0.2.1** | `9dda3e5` | **استرجاع بروتوكول وزارة الصحة و`SFDA MDS-G008` — كلاهما عامّ، وثلاث جولات بحث عميق قالت خطأً إنهما غير متاحين.** صُحِّحت أرقام المستويات المرجعية (خطأ في ٣ صفوف من ٤، وصف مُختلَق للـ HRCT). أُثبت المرسوم الملكي **60057** كأساس مُلزِم |
| **v0.2.0** | `2374deb` | دمج ثلاث جولات بحث في ٣٤ سجلاً و٦ ثغرات. استُخرجت الأرقام من **٢٦ صورة نقطية** في تقرير Gemini؛ أول تصيير خرج أسوداً بالكامل (PNG شفاف على RGB) فأُصلح بـ `alpha_composite` |

---

## v0.1.0 — الأساس · 2026-08-29

**`1ff1047`** — هيكلة `docs/` و`kb/` و`app/` و`reference/` و`scenarios/`.
استيراد إطار ITU الرسمي (١٣ بُعداً، ٦ عوامل، ١١١ مقياساً) وخمس وثائق أوّلية من
مستودع المنظِّمين. **حُذف قسم التعليم والقُصَّر (٧٣ سطراً)** من قاعدة المعرفة —
لا يوجد في المشروع قاصر ولا طالب، ووجوده كان يفضح متناً منسوخاً من مشروع آخر.
محوّل نموذج لغوي محايد المزوّد (`app/llm.py`).

---

## قرارات ثابتة عبر كل الإصدارات

| القرار | السبب |
| :--- | :--- |
| **لا نموذج لغوي في مسار القرار** | ادعاء السلامة ينهار في التحكيم لو كان الحجب احتمالياً. `P` كود حتمي، والنموذج يكتب الشرح فقط |
| **التضمينات محلية (ChromaDB)** | لا مفتاح ولا شبكة — فالاسترجاع والحجب يعملان لو سقطت شبكة المكان أثناء العرض |
| **الحجب المشروط لا المطلق** | الحجب المطلق كان سيخالف البروتوكول الذي يفرضه النظام نفسه |
| **٣ أبعاد جاهزية من ١٣** | الإطار يطلب أدلّة؛ وحشو القائمة يُضعف الثلاثة التي نملك دليلها |
| **صفر مفاتيح في المستودع** | شرط تحكيم صريح. المفتاح في `.env` وحده، و`.env` مستثنى من Git |

---

## المتبقّي قبل التسليم

- [ ] **تسجيل الفيديو** وفق `VIDEO-SCRIPT.md` — مع ترجمة نصية
- [ ] **تصدير التقرير PDF** والتأكد من أنه **٥ صفحات بالضبط**
- [ ] **رفع المستودع عاماً** برخصة MIT
- [ ] **تدوير مفتاح الـ API** الذي ظهر في المحادثة قبل أن يصير المستودع عاماً
