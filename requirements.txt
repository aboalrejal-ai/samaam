# عقدة النموذج (M) — أي مزوّد متوافق مع OpenAI (Z.AI افتراضياً)
openai==3.6.0

# قاعدة المعرفة والتضمين المحلي (PP + استرجاع)
chromadb==1.5.9
sentence-transformers==6.0.0

# قراءة الوثائق الأصلية (SRC)
pypdf==6.16.2

# الخدمة (C + SINK)
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
